import React, { FC } from 'react';

import {
    Container,
    TitleWrapper,
} from "./style";

const Tools: FC = () => {

    return (
        <Container>
            <TitleWrapper>{"Tools"}</TitleWrapper>

        </Container>
    );
};

export default Tools;
